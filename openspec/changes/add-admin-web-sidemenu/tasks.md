## 1. api: loosen read access on four endpoints

- [x] 1.1 `src/handlers/app_users.rs::list`: extractor `RequireAdmin` → `RequireActiveOrg`; confirmed no other implicit admin-only assumption in the handler body (pure read)
- [x] 1.2 `src/handlers/checkin.rs::list_users`: same extractor swap
- [x] 1.3 `src/handlers/checkin.rs::list_user_events`: same extractor swap
- [x] 1.3b `src/handlers/location_tracking.rs::list_locations` (`GET /checkin/users/:id/locations`): same extractor swap — **added mid-apply, not in the original task list**. design.md's Resolved Questions already decided the trajectory page (`checkin/[appUserId]/trajectory.vue`) opens to member with content identical to admin, but that page's data comes from this endpoint, which the original three-endpoint list missed entirely; member would have hit `FORBIDDEN` loading the page. `export_locations` (`GET /checkin/users/:id/locations/export`, xlsx download) stays `RequireAdmin` — confirmed with the user that bulk location-data export is treated as more sensitive than viewing the map, so only the UI export button gets admin-gated (task 5.3), the endpoint itself is untouched.
- [x] 1.4 Confirmed `create`/`update`/`password_reset` in `app_users.rs`, `force_checkout`/`update_settings` in `checkin.rs`, and `export_locations` in `location_tracking.rs` are untouched and still `RequireAdmin`; `cargo build` compiles clean

## 2. api integration tests

- [x] 2.1 `GET /app-users`: `tests/app_users_list.rs::member_lists_app_users_identically_to_admin` — replaced the old `member_cannot_list_app_users` test (asserted `FORBIDDEN`, now stale) with a byte-for-byte body-equality check between admin and member responses
- [x] 2.2 `GET /checkin/users`: `tests/checkin_admin_views.rs::member_views_checkin_board_identically_to_admin` — replaced the old `member_cannot_view_checkin_board` test the same way
- [x] 2.3 `GET /checkin/users/:id/events`: `tests/checkin_admin_views.rs::member_views_event_history_identically_to_admin` (new test, no prior member-forbidden test existed for this one)
- [x] 2.4 Regression confirmed via full `cargo test` run (no code changes needed — these handlers weren't touched): existing `member_cannot_create_app_user` (app_users_create.rs), `member_cannot_update_app_user` (app_users_update.rs), `member_cannot_reset_password` (app_users_password_reset.rs), the force-checkout member-forbidden test in checkin_force_checkout.rs, and the `PATCH /orgs/me/settings` member-forbidden test in checkin_transfer_toggle.rs all still pass
- [x] 2.5 Cross-Org isolation for a member caller: `tests/app_users_list.rs::member_cross_org_isolation_still_holds`, `tests/checkin_admin_views.rs::member_cross_org_checkin_board_still_isolated`, `tests/checkin_admin_views.rs::member_cross_org_event_history_still_not_found`, `tests/location_tracking_list.rs::member_cross_org_app_user_id_still_returns_404` (all new)
- [x] 2.6 `GET /checkin/users/:id/locations` (added in 1.3b): `tests/location_tracking_list.rs::member_lists_pings_identically_to_admin` replaces the old `member_role_blocked` test (asserted `FORBIDDEN`, now stale) with a byte-for-byte body-equality check; full `cargo test` run confirms no other test relies on this endpoint rejecting `member`

## 3. admin-web: shared layout

- [x] 3.1 New `layouts/default.vue`: side navigation panel (`OrgSwitcher` + role-computed nav link list) + `<slot />` content area; wired via `app.vue`'s `<NuxtLayout><NuxtPage /></NuxtLayout>` (previously bare `<NuxtPage />`, no layout system was active at all)
- [x] 3.2 `navItems` computed property inside the layout, not duplicated — base set for everyone, admin-only additions pushed conditionally on `auth.isAdmin.value`
- [x] 3.3 Pending join-request badge moved into the layout (same `refreshPendingJoinCount` / 30s `setInterval` logic relocated from `pages/index.vue`), rendered only when `item.badge` is set (which only the admin-only 加入申請 entry ever carries)
- [x] 3.4 Collapsible panel: `-translate-x-full` by default, `md:static md:translate-x-0` always-visible on wide viewports, a `sidebarOpen` ref toggled by a hamburger button + backdrop on narrow viewports — pure Tailwind transforms, no new dependency
- [x] 3.5 Structurally guaranteed, not just tested: the sidebar and the page content (`<slot />`) are separate, permanently-mounted DOM subtrees; collapsing/expanding only toggles a CSS transform class on the `<aside>`, never touches or remounts the content area — will also confirm visually in the group 6 manual smoke

## 4. admin-web: migrate pages onto the shared layout

For each page below: remove its own `<header>` (Org switcher + 回首頁), keep the page's own title/content, rely on `layouts/default.vue` for navigation.

- [x] 4.1 `pages/index.vue`: removed the header (title/email/OrgSwitcher/logout — all now in `layouts/default.vue`) and the entire redundant「管理員工具」nav-link cluster section (every link it had is now in the sidebar); kept the page title as a plain `<h1>`, 組織資訊/打卡設定/離開組織 sections untouched
- [x] 4.2 `pages/members.vue`: removed header (OrgSwitcher/冷卻管理 link/回首頁), kept plain title; also did 5.1 in the same pass (removed the admin-only redirect guard, gated the row action buttons behind `auth.isAdmin.value`, unused `router` const removed)
- [x] 4.3 `pages/cooldowns.vue`: removed header (OrgSwitcher/成員管理 link/回首頁), kept plain title; admin-only guard untouched per 5.4
- [x] 4.4 `pages/app-users/index.vue`: removed header, kept plain title; did 5.2 in the same pass
- [x] 4.5 `pages/checkin/index.vue`: removed header (OrgSwitcher/App使用者 link/回首頁), kept plain title; did 5.3's guard-removal + force-checkout gating in the same pass
- [x] 4.6 `pages/checkin/[appUserId]/index.vue`: removed header's OrgSwitcher/回首頁 (kept the 返回打卡看板/查看軌跡 contextual links); did 5.3's guard-removal in the same pass (page has no mutating controls, pure read)
- [x] 4.7 `pages/checkin/[appUserId]/trajectory.vue`: no OrgSwitcher/回首頁 existed on this page to remove; did 5.3's guard-removal (the old `if (auth.isAdmin.value && ...) loadDay()` gate silently stuck member on an infinite loading spinner — this was the de facto guard for this page since it never had a `navigateTo('/')` redirect) and gated the 匯出 xlsx button behind `auth.isAdmin.value` per the location-tracking export decision in task 1.3b
- [x] 4.8 `pages/settings/auth.vue`: removed the redundant「← 回儀表板」backlink (this page never had OrgSwitcher/回首頁ーit used a different single-backlink pattern); admin-only guard (inline amber notice, not a redirect) untouched per 5.4
- [x] 4.9 `pages/settings/api-tokens.vue`: removed header (OrgSwitcher/回首頁), kept plain title; unused `router` const removed; admin-only guard untouched per 5.4
- [x] 4.10 `pages/admin/join-requests.vue`: removed the redundant「← 返回首頁」backlink; admin-only guard untouched per 5.4
- [x] 4.11 `pages/orgs/new.vue`: no code change needed — this page never had OrgSwitcher/回首頁 duplication (its own header is a contextual「← 回首頁 / ← 返回」backlink for the onboarding flow, not the pattern being centralized); already in `ORG_AGNOSTIC_PATHS`, so it picks up `layouts/default.vue` automatically since it doesn't opt out with `layout: false`
- [x] 4.12 `pages/orgs/join.vue`: same as 4.11, no code change needed
- [x] 4.13 Confirmed via `definePageMeta({ layout: false })` added to all five (`login.vue`, `register.vue`, `no-org.vue`, `privacy.vue`, `download.vue`) — necessary because `layouts/default.vue` is Nuxt's convention-based default layout and auto-applies to every page that doesn't opt out; these pages' own content/markup is otherwise untouched

## 5. admin-web: open member access to the three read-only pages

- [x] 5.1 `pages/members.vue`: done alongside 4.2 above — guard removed, member now loads/renders the list, all row action buttons (role change, remove, owner-transfer) wrapped in one `v-if="auth.isAdmin.value"` container (owner-transfer was already additionally gated on `auth.isOwner.value`, a subset of admin)
- [x] 5.2 `pages/app-users/index.vue`: guard removed; three-way section split (external-auth info / admin create-form / member-only info message) added for the non-`isExternal` case; per-row action cell wrapped in `v-if="auth.isAdmin.value"` (with a `—` placeholder for member), unused `router` const removed
- [x] 5.3 Done alongside 4.5–4.7 above: guard removed on all three; force-checkout button gated on `auth.isAdmin.value` in `checkin/index.vue`; trajectory's 匯出 xlsx button gated on `auth.isAdmin.value` (export endpoint itself stays admin-only per task 1.3b)
- [x] 5.4 Confirmed: `cooldowns.vue` and `settings/api-tokens.vue` still have their `if (auth.isAdmin.value) { await load() } else { await navigateTo('/') }` redirect guard untouched; `settings/auth.vue` still has its inline amber-notice guard (`v-if="!auth.isAdmin.value"`) untouched; `admin/join-requests.vue` still has its `if (!auth.isAdmin.value) router.replace('/')` guard untouched — none of these four had their guard logic modified, only header markup

## 6. Docs & verification

- [x] 6.1 `cargo test` (full suite) clean; `cargo clippy --all-targets` clean; `cargo fmt` applied (one flaky testcontainer failure on a full-suite run — `unauthenticated_returns_401` in `location_tracking_self_list.rs` — reran clean in isolation and on a second full-suite run; unrelated to this change, Docker resource contention under parallel test binaries)
- [x] 6.2 admin-web `pnpm typecheck` clean; `pnpm test` 38/38 passed; `pnpm build` clean
- [x] 6.3 Manual browser smoke as admin, done with a real headless-Chromium session (Playwright, installed ad-hoc for this session) driving the actual running dev servers (api on :9090, admin-web on :3000) — not just curl/code review. Registered a fresh org, logged in via the real `/login` form, confirmed: full 8-item nav renders, join-request badge shows the correct pending count, approved the request through the real UI, visited all 7 admin nav destinations and confirmed each renders its expected `<h1>`, confirmed the hamburger control appears at a 375px viewport, and confirmed the sidebar's open/close toggle does not reset an in-progress form (typed into the slug-edit input, toggled the sidebar, value survived). **Caught and fixed a real bug this way**: the sidebar's `<aside>` mixed a static `class` attribute (`-translate-x-full`) with a `:class` binding (`translate-x-0` when open) — Vue merges rather than replaces these, so both ended up in the DOM simultaneously and Tailwind's stylesheet order (not markup order) decided which won, silently breaking the open state. Fixed by making the translate class fully conditional (`sidebarOpen ? 'translate-x-0' : '-translate-x-full'`); confirmed the fix via `getAttribute('class')` + `boundingBox()` (a `fullPage: true` screenshot of a `position: fixed` element is a known Chromium/Playwright rendering artifact and was not itself trustworthy — a viewport-only screenshot confirmed the real fix).
- [x] 6.4 Manual browser smoke as member, same real-browser session: registered a member via `mode=join`, admin approved, member logged in via the real form. Confirmed: sidebar shows exactly the reduced 4-item set (成員管理／App使用者／打卡看板／下載App), `/members` `/app-users` `/checkin` are all reachable (stayed on the URL, no redirect) and render data with mutating controls hidden (screenshots confirm no role-change/remove/create/reset-password/force-checkout buttons, just read-only rows), `/cooldowns` and `/admin/join-requests` redirect to `/`, `/settings/api-tokens` redirects to `/`, and `/settings/auth` is reachable but shows its inline "只有管理員可以檢視或變更驗證設定" notice (this page was already built to gate inline rather than redirect — confirmed as expected, not a regression). Also fixed a minor copy bug found via screenshot: the empty-`App 使用者`-list message told member to click a "新增 App 使用者" button they don't have; now conditioned on `auth.isAdmin.value`.
