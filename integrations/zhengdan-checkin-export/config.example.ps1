# Copy this file to config.ps1 (same directory as export.ps1) and fill in
# real values. config.ps1 is gitignored — it holds a live API token and
# must never be committed. See README.md for how to obtain the token.

$ApiBaseUrl = 'https://bandao-api.ccmos.tw'
$ApiToken = 'bandao_at_REPLACE_ME'
$TargetFolder = 'C:\Zhengdan\Import'
